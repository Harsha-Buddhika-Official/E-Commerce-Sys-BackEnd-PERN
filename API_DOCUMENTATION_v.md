# API Documentation (v): Attributes grouped by category

This file documents only the `GET /api/v/attributes/grouped/:categoryId` route.

For full details, examples, and a curl snippet see: [docs/attribute-grouped-by-category.md](docs/attribute-grouped-by-category.md)

Summary:

- Method: `GET`
- Path: `/api/v/attributes/grouped/:categoryId`
- Auth: `Authorization: Bearer <token>` (roles: `super_admin`, `admin`)

Use the linked doc for request/response examples and notes about alternate query/body inputs.

